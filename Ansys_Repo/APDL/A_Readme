coord：就是coordinates的数据，也就是坐标的数据。
ele：就是单元数据，里面有每个点的索引数据。
以上两个数据不论是几个static structural都一样，所以是通用的。

disp：是deformation，也就是变形数据
stress：是应力数据
以上两个数据多个static structural和单个是不一样的输出方式

2020.12.12
坐标和元素的输出使用20200627里面的，其实20200613里面的和它一样，就是多个结果文件的坐标和元素
的输出是新的。单个和多个结果文件的坐标和元素输出方式一样。

2020.12.13
1、注意有时候ansys划分出来的单元不是187或者185单元，比如可能是186单元，就会输出不出来。
2、beam188的ele文件和solid的不一样，一行单元只有3个节点，所以ele的输出程序在20200627中重新写了一下。
不过坐标输出的程序是一样的，只需把单元号从187/185改为188就可以。
就目前的观察来看，beam的每个单元的前两个节点是beam188的，后边一个节点是CLOAD201的。

2020.12.19
一定要注意，在输出多个rst的结果是，命名规则很重要，中间必须加下划线。
比如1_2表示这个文件存储的是第一个rst中的第二个子步的结果数据。
如果不加下划线，超过10个rst就会有1_10变成110，它会放在10_9后边，因为10_9变成了109。

其实这个问题在20200627的文件中就改了，但是后续要注意。所以要对python的读取程序也要修改。